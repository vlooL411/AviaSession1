<UserControl x:Class="AviaSession1.AdaptiveLTB"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AviaSession1"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="2.5*"/>
			<ColumnDefinition Width="7.5*"/>
		</Grid.ColumnDefinitions>
		<Viewbox>
			<Label x:Name="Head" HorizontalAlignment="Center" VerticalAlignment="Center"/>
		</Viewbox>
		<Viewbox Grid.Column="1">
			<TextBox x:Name="Text" HorizontalAlignment="Center" MinWidth="100" VerticalAlignment="Center"/>
		</Viewbox>
	</Grid>
</UserControl>
